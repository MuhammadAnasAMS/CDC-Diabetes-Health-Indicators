# CDC-Diabetes-Health-Indicators
An end-to-end ML pipeline predicting diabetes risk using the CDC Health Indicators dataset. Benchmarks XGBoost, Random Forest, SVM, and MLP with full EDA, GridSearchCV hyperparameter tuning, and comprehensive evaluation (ROC/AUC, confusion matrices).
